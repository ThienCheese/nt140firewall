# Environment variables for the DNS Firewall

# --- Cloudflare Tunnel Configuration ---
# Your domain name managed by Cloudflare
YOUR_DOMAIN_NAME=your-domain.com

# The token for your Cloudflare Tunnel
# Get this from the Cloudflare Zero Trust Dashboard
CLOUDFLARE_TUNNEL_TOKEN=

# --- Admin Credentials ---
# The password for accessing the web dashboard
ADMIN_PASSWORD=YourStrongPassword
ADMIN_HASHED_PASSWORD=$2b$12$examplehashedpasswordhere
# --- DNS Server Configuration ---
# Primary upstream DNS server
UPSTREAM_DNS_1=1.1.1.1

# Secondary upstream DNS server
UPSTREAM_DNS_2=1.0.0.1

# --- Sinkhole Configuration ---
# The IP address of the sinkhole server.
# For internal Docker traffic, this should be the Caddy container's IP.
# Since we are using Docker networks, we can use the service name.
# However, the Python app needs a real IP. We'll use the gateway or a loopback.
# For LAN clients, this should be the host's IP where Docker is running.
# Let's use a placeholder that Caddy can serve.
SINKHOLE_IP=127.0.0.1 # Replace with your Docker host's LAN IP

# The IP address of your router (optional, for specific filtering rules)
ROUTER_IP=192.168.1.1
